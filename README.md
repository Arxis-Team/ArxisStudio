# ArxisStudio

Кроссплатформенный движок разработки прикладных программ: оболочка, из которой
всё остальное приходит плагинами. В самой студии нет ни одной панели и ни одного
формата документа — она умеет зоны, вкладки, меню, статус-бар и один контракт
расширяемости, по которому плагин заявляет свои панели, команды, редакторы
документов, инспекторы и рисовальщики свойств.

Дизайнер форм, дерево решения и запуск проекта в репозитории были — и сняты со
сборки 2026-08-28: держать их поверх платформы, которая сама ещё не готова,
значит проверять её там, где она ничего не решает. Вернутся они плагинами — на
тех же правах, что и чужие. Что именно снято и почему — в
[плане](docs/plan.md), этап 17.

## Состав

Студия собрана из библиотек, каждая со своим репозиторием; в этом они подключены
подмодулями в `external/`.

| Репозиторий | Что делает |
|---|---|
| [ArxisStudio.Controls](https://github.com/Arxis-Team/ArxisStudio.Controls) | Контролы студии (`Ax*`) — из них строятся и её интерфейс, и плагины |
| [ArxisStudio.Themes.Arxis](https://github.com/Arxis-Team/ArxisStudio.Themes.Arxis) | Тема: тёмная и светлая палитры, шаблоны контролов |
| [ArxisStudio.Fonts.Cascadia](https://github.com/Arxis-Team/ArxisStudio.Fonts.Cascadia) | Моноширинный шрифт, вшитый в сборку |

Ещё три подмодуля — [Markup](https://github.com/Arxis-Team/ArxisStudio.Markup)
(разметка без потерь), [ProjectSystem](https://github.com/Arxis-Team/ArxisStudio.ProjectSystem)
(модель решения) и [DesignEditor](https://github.com/Arxis-Team/ArxisStudio.DesignEditor)
(канва) — остаются зарегистрированными, но ни один проект на них не ссылается:
они ждут возвращения дизайнера и работы с решением. В `ArxisStudio.slnx` их нет —
решение перечисляет то, что собирается; работать с ними нужно их собственными
решениями, они лежат в корне каждого подмодуля.

В самом репозитории — приложение-оболочка (`src/ArxisStudio`), каркас окна и
настройки (`src/ArxisStudio.Shell`), контракт расширяемости (`src/ArxisStudio.Sdk`)
с анализатором и таргетом упаковки, хост плагинов
(`src/ArxisStudio.Extensibility`), встроенные модули (`src/Modules/`) и пример
внешнего плагина (`samples/Arxis.HelloPlugin`).

В оболочке нет ни одной панели и ни одного формата документа. Зоны, вкладки, меню
и статус-бар она умеет, а чем их наполнить — сообщают манифесты плагинов.
Доставок у панели две, а контракт один. Внешний плагин ставится в папку плагинов
и живёт в своём выгружаемом контексте загрузки; встроенный модуль приезжает
вместе со студией, его манифест лежит в сборке ресурсом, а сборка — в основном
контексте. Код между этими двумя режимами переносится без правок, и в
репозитории есть образец каждого: `samples/Arxis.HelloPlugin` и
`src/Modules/ArxisStudio.Modules.Sample`.

## Что уже работает

- **Welcome** — недавние проекты, шаблоны из установленных `dotnet new`, менеджер
  плагинов, настройки; тема и язык переключаются на лету.
- **Окно студии** — зоны со сплиттерами, вкладки документов, меню команд, статус-бар
  и тёмная со светлой темой; панели в зонах — только те, что принесли плагины.
- **Менеджер плагинов** — установка из папки и из `.axplugin`, обновление
  установкой поверх, включение, выключение и удаление; выключённым плагин
  остаётся и после обновления.
- **Модули и плагины** — панель, команда в меню и служба; свои рисовальщики свойств и свои
  инспекторы для типов контролов; редактор документов, которому оболочка отдаёт
  открываемый файл, не зная ни одного расширения; плагин поднимается по
  объявленному событию, а не при каждом старте; каждый в своём выгружаемом
  контексте загрузки.
- **Сбой плагина не роняет студию** — все вызовы чужого кода идут через один шов,
  который считает падения и на третьем подряд перестаёт плагин звать; панель,
  упавшая в раскладке, заменяется заглушкой с причиной и кнопкой перезапуска.
- **Упаковка плагина** — `dotnet build` раскладывает его в формат каталога и по
  просьбе пакует в `.axplugin` или кладёт прямо в папку студии для отладки.
- **Проблемы** — контракт для находок: код, сообщение, файл и строка. Сообщать их
  теперь некому, кроме плагинов, — своих источников у студии не осталось.

Чего пока нет: дизайнера форм, дерева решения и запуска проекта — они вернутся
плагинами; экрана Run/Debug с горячей перезагрузкой; проверки хоста при загрузке
плагина — она означала бы разбор чужого IL и ждёт подписи пакетов.
План со всеми этапами — в [docs/plan.md](docs/plan.md).

## Сборка

Нужен .NET SDK 10. Сборке нужны три подмодуля из пяти — контролы, тема и шрифт;
остальные два лежат в репозитории, но ни одним проектом не собираются.

```bash
git clone --recurse-submodules https://github.com/Arxis-Team/ArxisStudio.git
cd ArxisStudio
dotnet build ArxisStudio.slnx
dotnet run --project src/ArxisStudio
```

Если репозиторий уже склонирован без подмодулей, хватит нужных:

```bash
git submodule update --init external/ArxisStudio.Controls external/ArxisStudio.Themes.Arxis external/ArxisStudio.Fonts.Cascadia
```

Тесты:

```bash
dotnet test tests/ArxisStudio.Tests
```

## Плагины

Плагин — папка с манифестом `plugin.json` и сборкой рядом; та же папка, упакованная
в zip, — это файл `.axplugin`, который студия устанавливает через менеджер плагинов.
Плагин ссылается только на `ArxisStudio.Sdk` и библиотеку контролов: про саму
студию он ничего не знает и обращается к ней через `IStudioContext`.

```csharp
public sealed class HelloPlugin : StudioPlugin
{
    public override void Activate(IStudioContext context) =>
        context.Commands.Register("hello.greet", () =>
            context.Log.Write(StudioLogLevel.Info, "Hello", "Здравствуйте!"));
}
```

Рабочий пример с панелью, командой и службой — в
[samples/Arxis.HelloPlugin](samples/Arxis.HelloPlugin). Формат манифеста описан в
[плане](docs/plan.md), приложение A.

Тот же контракт со стороны студии — встроенный модуль
[src/Modules/ArxisStudio.Modules.Sample](src/Modules/ArxisStudio.Modules.Sample):
манифест `module.json` едет ресурсом внутри сборки, ссылок ему хватает тех же —
SDK и контролы, — а разница видна только в двух местах: список модулей объявлен
в самой студии, и выгрузить модуль отдельно нельзя. Перенести код модуля во
внешний плагин — значит переложить каталог и собрать `.axplugin`; в самом коде
не меняется ничего.

Собирается плагин обычным `dotnet build`: таргет упаковки приходит вместе с SDK и
раскладывает выход в формат каталога сам. Архив и отладочная установка — по
просьбе:

```bash
dotnet build samples/Arxis.HelloPlugin -p:AxPluginPack=true
```

```bash
dotnet build samples/Arxis.HelloPlugin -p:AxPluginDeploy=true
```

Первое кладёт рядом `arxis.hello.axplugin` — его студия ставит через менеджер
плагинов. Второе кладёт плагин прямо в папку студии, мимо менеджера: перед этим
её надо закрыть, папка заменяется целиком, а выключенным плагин останется и после
выкладки — это записано рядом, в `.disabled.json`.

Общие контракты — SDK, контролы и Avalonia — в пакет не попадают: студия берёт их
из своего контекста загрузки, и копия рядом с плагином стала бы вторым
экземпляром того же типа.

Интерфейс плагина строится на контролах `Ax*`, а не на голых виджетах Avalonia:
панели студии и панели плагинов стоят рядом, и разнобой видно сразу. Правило
проверяется при сборке — анализатор `ARX0001` приходит вместе с SDK. Панели
раскладки, рамки, текст и фигуры он не трогает: своего оформления они не несут.

## Отладка самой студии

В Debug-сборку встроен [AvaDevTools](https://pavel-zheltiakov.github.io/AvaDevTools/):
`F12` открывает инспектор дерева, стилей и раскладки, а на петле поднимается
конечная точка MCP — по ней студией можно управлять из агента. Подробности —
в [docs/devtools.md](docs/devtools.md).

## Документы

- [docs/plan.md](docs/plan.md) — план разработки, принятые решения, формат плагина.
- [docs/design-spec.md](docs/design-spec.md) — выжимка дизайна: палитры, шрифты,
  метрики, инвентарь экранов.
- [docs/devtools.md](docs/devtools.md) — отладка интерфейса и управление студией
  из агента.
